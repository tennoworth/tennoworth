{
  "request_delay_ms": 340,
  "note": "Post-request sleep for the WFM scrape, ~3 req/s. The single source for the scrape pacing (companion/wfm-scrape/src/http.rs REQUEST_DELAY). Python is retired; this fixture documents the constant so a bump is deliberate. Raising it slows a ~45-minute full scrape; lowering it risks WFM rate-limiting the box."
}
